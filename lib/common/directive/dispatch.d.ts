import { IComponent } from "../types/component";
import { IDirective } from "../types/directive";
export declare function DispatchDirective(component: IComponent | string, element: HTMLElement, directive: IDirective, repeats?: number): boolean;
